
class menu extends Pantalla {
    draw() {
        fill(100);
        rect(0, 0, width, height);
        fill(0);
        textAlign(CENTER)
        text('MENÚ DE NAVEGACIÓN', width/2, height/2);
    }
}
